package com.dayjoy.fit90

import io.flutter.embedding.android.FlutterFragmentActivity

// Health Connect's permission flow requires an AndroidX FragmentActivity host,
// so we extend FlutterFragmentActivity instead of FlutterActivity.
class MainActivity : FlutterFragmentActivity()
